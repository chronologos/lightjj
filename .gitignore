# Binaries
/lightjj
*.exe

# Frontend build output
# Bundled into the release binary via `go build -tags embed`; not tracked in git.
# Bare `go install` builds the !embed (default) variant which serves a static
# help page pointing at the release binaries. See cmd/lightjj/frontend_stub.go.
cmd/lightjj/frontend-dist/
frontend/node_modules/
frontend/.svelte-kit/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Local tooling — never commit
.claude/
.superpowers/
